# AiBnBClean

## development setup

on raspberry pi

```bash
sudo apt install chromium-browser
sudo apt install chromium-chromedriver
```

### python virtual environment

```pwsh
git clone <clone url>
cd <cloned code folder>
python -m venv .venv
. ./.venv/bin/activate
which pip
pip install -r requirements.txt
```
